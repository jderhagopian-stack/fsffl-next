// Temporarily disabled while isolating the iPhone Safari startup lockup.
// The base page must remain responsive before resume-specific recovery is reintroduced.
// Keep this module intentionally inert so the current HTML contract remains stable.
window.fsfflMobileSafariRecoveryDisabled=true;
