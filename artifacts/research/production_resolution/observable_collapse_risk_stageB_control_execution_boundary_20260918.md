# Observable Collapse-Risk Stage B — Control Extraction Execution Boundary

Date: 2026-09-18

Identity materialization is complete (398/398 distinct GSIS IDs; all 661 coordinates). Frozen leverage order remains unchanged.

A bounded, reproducible Stage-B structural-control workflow has been added and explicitly triggered. It is designed to:
- recover the frozen Phase-2 q3 rows from durable artifact 10486530017;
- retrieve nflverse weekly-roster source-season snapshots for 2014–2020;
- hash every roster snapshot;
- derive only source-time roster status/non-active-share features;
- compare collapse and non-collapse outcomes by horizon;
- persist the row-level collapse features and control summary;
- perform no model fitting.

At this checkpoint the GitHub Actions result has not yet materialized on the branch. No causal conclusion or Stage-C feature eligibility decision is made from an unobserved workflow result.

No model fitting, current-board tuning, holdout selection, main/PR147/production/Shapley change.
