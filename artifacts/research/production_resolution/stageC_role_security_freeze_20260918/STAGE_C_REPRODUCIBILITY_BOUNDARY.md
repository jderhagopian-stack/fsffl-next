# Stage C — Frozen Representation and First Reproducibility Boundary

Date: 2026-09-18

## Authority
Stage C was authorized by management for one family only: continuous, position-aware late depth-chart role / starting-job security erosion. The representation was frozen before candidate evaluation at commit 524d689bac3783c6dc31b70ce253bf0262c05e2e.

## Frozen scope
- QB: Y2/Y3
- WR: Y2/Y3
- RB: no adjustment
- TE: no adjustment
- no governed Y1 change

Continuous source-time inputs:
1. final-four mean depth rank
2. final-four starter share
3. late-half minus early-half starter share

No thresholds. Missing depth evidence implies no adjustment, not pessimistic imputation. The signal may operate only through future persistence / active-state / opportunity-state probability machinery, not direct suppression of conditional-production magnitude.

## Reproducible materialization completed
Workflow 35407039380 / artifact 10572372631 materialized the exact frozen inputs from nflverse weekly depth-chart releases for 2005–2022 and preserved per-season SHA-256 hashes. This establishes that the Stage-C signal itself is reproducibly operationalizable.

## Genuine fitting boundary
S1/S2 fitting has NOT begun.

The currently recovered B2a prediction artifact contains final expected points / retention but does not expose the internal routed future persistence/state/opportunity probabilities to which Stage C is required to attach. The recovered integrated I1 OOS artifact exposes state probabilities but omits player_id, so it cannot be deterministically joined to the newly materialized player-level depth signal. The earlier persistence-first artifact does retain player_id and state probabilities, but substituting one of those earlier candidate probability surfaces for the selected routed + M1a + two-prior-season Forecast would be a new methodological choice and would violate the directive.

Applying the Stage-C signal directly to B2a expected-point retention would also violate the frozen architecture placement by turning a role/security probability adjustment into direct magnitude suppression.

Therefore the legal next dependency is an exact player-keyed materialization of the selected frozen routed Forecast's pre-adjustment persistence/state/opportunity probabilities for the same chronological B2a evaluation rows, or sufficient code/artifacts to reproduce that materialization without choosing a substitute model.

No S1/S2 outcome metrics were inspected; no holdout/current board used; no main/PR147/production/Shapley change.
